
import 'package:flutter/material.dart';

class LooksTab extends StatelessWidget {
  const LooksTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext c) => const Center(child: Text('hello from Looks'));
}
