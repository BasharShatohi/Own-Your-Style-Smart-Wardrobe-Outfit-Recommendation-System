import 'package:flutter/material.dart';

class AppColors {
  // Original brand palette — unchanged.
  static const primary = Color(0xFFA8A8FF);
  static const background = Color(0xFFF5F7FE);
  static const textDark = Color(0xFF281A27);
  static const accentBlue = Color(0xFF3A5BF0);
  static const accentMint = Color(0xFF6BD8BA);
  static const textGrey = Color(0xFF979EA9);

  // Neutral helpers used by the shared theme. These are the tones already
  // present in the app, just named — they do not change the brand colour.
  static const primarySoft = Color(0xFFE6EEF7);
  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0xFFE5E7EB);
  static const danger = Color(0xFFE57373);
}
